apply from: rootProject.file('android-library.gradle')

dependencies {
    implementation (deps.simple_xml) {
        exclude group: "stax"
        exclude group: "xpp3"
    }

    // for support KmlDeserializerImpl here
    implementation project(':FlexibleClient')
}
